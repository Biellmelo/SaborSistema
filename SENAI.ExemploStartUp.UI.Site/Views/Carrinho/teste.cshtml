@model IEnumerable<SENAI.ExemploStartUp.Models.Models.ItemCarrinho>

@{
    ViewBag.Title = "teste";
    Layout = "~/Views/Shared/_LayoutCliente.cshtml";
}

<h2>teste</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CarrinhoDeCompras.CarrinhoDeComprasId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produto.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CarrinhoDeCompras.CarrinhoDeComprasId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produto.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemCarrinhoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemCarrinhoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemCarrinhoId })
        </td>
    </tr>
}

</table>
